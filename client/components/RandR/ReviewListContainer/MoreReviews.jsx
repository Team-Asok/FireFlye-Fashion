import React from 'react';

const MoreReviews = (props) => (
  <button onClick={props.onClick} id="more-reviews" type="button" text="More reviews +">More Reviews +</button>
);

export default MoreReviews;
